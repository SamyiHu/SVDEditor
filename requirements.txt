PyQt6>=6.5.0
# 可选：如果需要更好的XML处理
# lxml>=4.9.0

# AI 助手（可选依赖，未安装不影响其他功能）
# openai>=1.0.0
# anthropic>=0.30.0

# 数据手册导入（可选依赖，未安装不影响其他功能；缺失时导入向导会提示安装）
# Parser 包需先 pip install -e ../Parser （或设环境变量 SVDEDITOR_PARSER_PATH）
# parser>=0.1.0
# openpyxl>=3.0.0
# python-docx>=1.1.0
# pdfplumber>=0.9.0
# pymupdf>=1.23.0
# pyyaml>=6.0

# 测试依赖
pytest>=7.0.0
pytest-qt>=4.0.0
pytest-cov>=4.0.0
