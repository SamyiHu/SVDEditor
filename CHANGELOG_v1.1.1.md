# SVD Editor 更新日志

## v1.1.1（2026-06-30）

### 🔧 数据安全（重点修复）
- **修复中断修改后保存丢失**：改中断的值/描述/关联外设（特别是继承外设如 UART2 derivedFrom UART3），保存后再打开会还原。根因是生成器对继承外设跳过了中断写入。
- **修复多文档数据互相覆盖**：编辑文档A后打开文档B，返回发现A被B覆盖；关闭标签页时编辑丢失。根因是打开新文档后活动文档指针未同步、关闭标签页未保存当前编辑。
- **修复重新打开同一文件时编辑被还原**：删除内容后不保存，再次打开同一文件，编辑被磁盘原始数据覆盖。
- **修复 AI 保存死锁/报错**：AI 回答完保存时卡死，弹窗后报 `argument 2 has unexpected type 'NoneType'`。根因是跨线程阻塞等待超时后静默返回 None。

### ✨ 功能增强
- **寄存器支持拖拽排序**：此前只有外设和位域能拖拽，寄存器拖拽会变成多选。现已补全（含撤销/重做）。
- **位域表双击空白新建位域**：位域列表表格双击空白处直接弹出新建位域对话框。
- **中断修改自动刷新**：AI 或手动修改中断后，中断表格自动刷新，无需手动切换标签页。
- **多选拖拽保护**：多选状态下禁止拖拽排序（避免只移动第一个选中项），Ctrl+点击用于多选。

### 🎨 界面优化
- **修复树状图选中/悬停时展开箭头消失**：箭头改用纯 QSS 矢量图标渲染，彻底消除闪烁和性能问题。
- **树状图 hover 重绘性能优化**：从全量重绘改为局部刷新。

### ✅ 质量
- 新增多文档数据持久化测试套件（9 场景 21 断言），防止数据丢失问题回归。
